[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "fr_load_forecast"
version = "0.1.0"
description = "French electricity load forecasting utilities."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
